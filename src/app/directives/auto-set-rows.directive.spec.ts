import { AutoSetRowsDirective } from './auto-set-rows.directive';

describe('AutoSetRowsDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoSetRowsDirective();
    expect(directive).toBeTruthy();
  });
});
