import { HighlightKeyWordsPipe } from './highlight-key-words.pipe';

describe('HighlightKeyWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightKeyWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
